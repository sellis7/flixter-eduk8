<header>
</header>
<div class="bounding-box col-xs-10 col-xs-offset-1">
	<h1>New Lesson here</h1>
	<hr />
	<%= simple_form_for @lesson, :url => instructor_section_lessons_path(current_section) do |f| %>
		<%= f.input :title %>
		<br />
		<%= f.input :subtitle %>
		<br />
		<%= f.submit "Add Lesson", :class => 'btn btn-primary' %>
	<% end %>
</div>
